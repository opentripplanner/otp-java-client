query {
    stop(id: "%s") {
        name
        gtfsId
        lon
        lat
        code
        vehicleMode
        zoneId
        parentStation {
            gtfsId
            name
            code
        }
    }
}