query {
    stops(name: "%s") {
        name
        gtfsId
        code
        vehicleMode
        zoneId
        parentStation {
            gtfsId
            name
            code
        }
    }
}
